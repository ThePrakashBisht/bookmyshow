# Server Configuration
server:
  port: 8081

# Application Name
spring:
  application:
    name: user-service

  # Database Configuration
  datasource:
    url: jdbc:postgresql://localhost:5433/userservice_db
    username: postgres
    password: postgres123
    driver-class-name: org.postgresql.Driver

  # JPA/Hibernate Configuration
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

# JWT Configuration
# Base64 encoded secret key (at least 256 bits for HS256)
jwt:
  secret: dGhpc2lzYXZlcnlsb25nc2VjcmV0a2V5Zm9yand0dG9rZW5nZW5lcmF0aW9udGhhdGlzYXRsZWFzdDI1NmJpdHNsb25n
  expiration: 86400000

# Logging
logging:
  level:
    com.bookmyshow: DEBUG
    org.springframework.security: DEBUG